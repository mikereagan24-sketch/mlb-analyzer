node_modules/
data/
.env
*.db

# Scratch / debug probes (one-off analysis, not part of the app)
probe-*.cjs
*-test.cjs
check-*.js
today-impact.cjs

# CSV scratch output
frv-*.csv
forecasts.csv

# Local editor/agent
.claude/

# Additional frv scratch (any extension)
frv-*.cjs
frv-*.py
refresh-db.sh

# Scratch analysis dir and one-off diagnostics. These were untracked for
# months and a `git add -A` swept ~90 of them (including sqlite WAL/SHM
# backups) into a PR on 2026-08-22. Ignored so that cannot recur.
tmp/
diag*.js
*.db-shm
*.db-wal
sweep-*.json
